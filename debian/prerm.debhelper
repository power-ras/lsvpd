# Automatically added by dh_installinit
if [ -x "/etc/init.d/vpdupdater" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d vpdupdater stop || exit 0
	else
		/etc/init.d/vpdupdater stop || exit 0
	fi
fi

update-rc.d vpdupdater remove

# End automatically added section
