# Automatically added by dh_installinit
if [ -x "/etc/init.d/vpdupdater" ]; then
	update-rc.d vpdupdater start 99 S . >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d vpdupdater start || exit 0
	else
		/etc/init.d/vpdupdater start || exit 0
	fi
fi
# End automatically added section
